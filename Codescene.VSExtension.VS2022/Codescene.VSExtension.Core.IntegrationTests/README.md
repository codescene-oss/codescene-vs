## VS Extension Core Integration Tests

This project contains integration tests for the VS Extension Core components of the Codescene Visual Studio Extension.

### Prerequisites

The CLI executable is required to run these tests. Ensure that it is built and accessible in the expected location. 
It will use the same location as specified in the Codescene.VSExtension.Core project, added to this project via linked file reference.