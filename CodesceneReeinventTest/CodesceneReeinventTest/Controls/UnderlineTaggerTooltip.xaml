<UserControl x:Class="CodesceneReeinventTest.Controls.UnderlineTaggerTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodesceneReeinventTest.Controls"
             mc:Ignorable="d" 
             >
    <Grid HorizontalAlignment="Stretch">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Foreground="{x:Static SystemColors.ControlLightBrush}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Category}" />
                <TextBlock Text=" ("> </TextBlock>
                <TextBlock Text="{Binding Details}"/>
                <TextBlock Text=") "></TextBlock>
                <TextBlock Text="CodeScene (" FontWeight="Light"></TextBlock>
                <TextBlock>
                    <Hyperlink  Command="{Binding YourCommand}" CommandParameter="{Binding Category}">
                         <Run Text="{Binding Category}" />
                    </Hyperlink>
                </TextBlock>
                <TextBlock Text=") " FontWeight="Light"></TextBlock>
            </StackPanel>

        </ScrollViewer>

    </Grid>
</UserControl>
